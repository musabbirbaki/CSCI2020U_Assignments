apply plugin: "java"

sourceSets{
    main.java.srcDir "src/sample"
}

jar{
    manifest.attributes "Main-Class": "sample.Main"
}

task copyTask(type: Copy) {
    from 'src/sample/sample.fxml'
    into 'build/classes/java/main/sample'
}
